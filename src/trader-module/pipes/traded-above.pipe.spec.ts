import { TradedAbovePipe } from './traded-above.pipe';

describe('TradedAbovePipe', () => {
  it('create an instance', () => {
    const pipe = new TradedAbovePipe();
    expect(pipe).toBeTruthy();
  });
});
